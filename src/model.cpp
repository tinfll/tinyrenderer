#include <fstream>
#include <sstream>
#include <model.h>
/*
Model::Model(const std::string filename) {
	std::ifstream in;
	in.open(filename, std::ifstream::in);
	while (!in.eof()) {
		std::getline(in, line);
	}
}*/